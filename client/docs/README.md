# client/ docs

How-tos and deep dives for `@devdigest/web`. The route map and stack overview
stay in [`../README.md`](../README.md); this folder holds everything longer
than a README section.

| Doc | Topic |
|-----|-------|
| —   | _none yet_ |

Adding a doc: one file per topic (`<topic>.md`), indexed above in the same change.
