import { SkillsListView } from "./_components/SkillsListView";

/* Route: /skills (Skills Lab — Skills list). Thin route entry — the view, its
   create/import modals, preview drawer, styles, constants, helpers and i18n
   are colocated under _components/SkillsListView. */
export default function SkillsPage() {
  return <SkillsListView />;
}
