# e2e/ docs

How-tos and deep dives for `@devdigest/e2e`. The flow format, runner knobs, and
coverage table stay in [`../README.md`](../README.md); this folder holds
everything longer than a README section.

Note: in this module `specs/` is the executable agent-browser flows
(`NN-name.flow.json`) — the flows themselves are the specs; design notes about
them go here.

| Doc | Topic |
|-----|-------|
| —   | _none yet_ |

Adding a doc: one file per topic (`<topic>.md`), indexed above in the same change.
